/**
 * `template-typescript` options
 */
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface Options {}

/**
 *
 * @example
 * ```js
 * ```
 */
export default function templateTypeScript(
  value: unknown,
  options?: Options,
): object
